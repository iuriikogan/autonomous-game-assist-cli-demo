apiVersion: v1
kind: ServiceAccount
metadata:
  name: game-assist-agent-runner
  namespace: game-assist
  annotations:
    # Replace <PROJECT_ID> with your actual GCP Project ID
    iam.gke.io/gcp-service-account: game-assist-runner-gsa@YOUR_GCP_PROJECT_ID.iam.gserviceaccount.com
  labels:
    environment: dev
    owner: ikogan
    cost-center: gaming-assist-ai
    managed-by: manual
